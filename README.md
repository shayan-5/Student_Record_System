# Student_Record_System
Student Record System is a Maven-based Java project that uses JDBC and MySQL to perform CRUD operations on student data. The project demonstrates database connectivity, SQL query execution, prepared statements, and efficient data management through a command-line interface.
